<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>

  <style>
    .wrap {
      display: flex;
      flex-direction: row;
      justify-content: center;
      padding-top: 40px;
      gap: 10px; /* 카드 간격 추가 */
      flex-wrap: wrap; /* 카드들을 여러 줄로 배치 */
    }

    .card-wrap {
      width: 18rem; /* 가로 */
      margin-bottom: 1rem; /* Bootstrap 카드 하단 여백 설정 */
      border: 1px solid #dddddd; /* 테두리 설정 */
      border-radius: 0.5rem; /* Bootstrap 카드 테두리 둥글기 설정 */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* 그림자 추가 */
      transition: transform 0.2s; /* 부드러운 애니메이션 */
      background-color: white; /* 카드 배경색 */
      overflow: hidden; /* 내용이 넘치지 않도록 처리 */
    }

    .card-wrap:hover {
      transform: translateY(-10px); /* Hover 시 위로 살짝 올라가도록 설정 */
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Hover 시 그림자 강조 */
    }

    .card-title, .card-content {
      padding: 1rem; /* 내용 패딩 설정 */
    }

    .card-title {
      font-size: 1.25rem; /* 제목 폰트 크기 설정 */
      font-weight: bold; /* 제목 폰트 굵기 설정 */
      color: #05C208; /* 제목 색상 설정 */
      margin-bottom: 0.75rem; /* 제목 하단 여백 설정 */
    }

    .card-content {
      font-size: 1rem; /* 내용 폰트 크기 설정 */
      color: #6c757d; /* 내용 색상 설정 */
    }
</style>

</head>
<body>
  <div class="wrap">

      <% for(let i = 0; i < data.length; i++) { %>

        <a href="/detail/<%= data[i].id %>">
          <div class="card-wrap">
            <div class="card-title"><%= data[i].title %></div>
            <div class="card-content"><%= data[i].content %></div>
          </div>
        </a>

      <% } %>
    </div>
</body>
</html>